---
name: "{{ current_env.environmentName }}-postgresql-reporting"
credentialsId: ""
labels:
- "solutionInstance-{{ current_env.environmentName }}"
- "solution-{{ current_env.tenant }}"
isServerSideMerge: false
cleanInstallApprovalRequired: true
mergeDeployParametersAndE2EParameters: false
deployParameters:
  dbaas:
    labels:
      rep_balancing_rule: postgresql-reporting
  postgresPassword: "${creds.get(\"postgresql-reporting-cred\").password}"
  postgresUser: "${creds.get(\"postgresql-reporting-cred\").username}"
e2eParameters: {}
technicalConfigurationParameters: {}
deployParameterSets:
  - postgresql-common
  - postgresql-dbaas
e2eParameterSets: []
technicalConfigurationParameterSets: []

