---
name: "{{ current_env.environmentName }}-zookeeper-streaming"
credentialsId: ""
labels:
- "solutionInstance-{{ current_env.environmentName }}"
- "solution-{{ current_env.tenant }}"
isServerSideMerge: false
cleanInstallApprovalRequired: true
mergeDeployParametersAndE2EParameters: false
deployParameters:
  global:
    secrets:
      zooKeeper:
        adminUsername: "${creds.get(\"zookeeper-streaming-admin-cred\").username}"
        adminPassword: "${creds.get(\"zookeeper-streaming-admin-cred\").password}"
        clientUsername: "${creds.get(\"zookeeper-streaming-client-cred\").username}"
        clientPassword: "${creds.get(\"zookeeper-streaming-client-cred\").password}"
e2eParameters: {}
technicalConfigurationParameters: {}
deployParameterSets:
  - zookeeper
e2eParameterSets: []
technicalConfigurationParameterSets: []

