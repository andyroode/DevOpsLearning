inventory:
  environmentName: "e01"

  clusterUrl: test-val.com
envTemplate:
  additionalTemplateVariables:
    key: value
  envSpecificParamsets:
    cloud:
      - cloud-specific
      - integrations
      - paramsetA
    main:
      - toms-params
      - internal-base-ns
      - paramsetB
