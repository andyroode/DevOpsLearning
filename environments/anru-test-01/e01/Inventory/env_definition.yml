inventory:
  environmentName: "e01"

  clusterUrl: test-val.com
envTemplate:
  additionalTemplateVariables:
    key: value
  envSpecificParamsets:
    cloud:
      - cloud-specific
      - paramsetA
      - integrations
    main:
      - paramsetB
      - internal-base-ns
      - toms-params
